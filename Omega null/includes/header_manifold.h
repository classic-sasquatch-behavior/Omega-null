#pragma once

//header_manifold has been separated out from manifold.h so that I can include it safely with my object headers. I'm pretty sure
//it would have been fine either way, but this way feels less sketchy because I dont have to be relying as much on #pragma once.

#include"manifold/external_libraries.h"
#include"manifold/macros.h"